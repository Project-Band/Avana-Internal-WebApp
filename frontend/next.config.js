/** @type {import('next').NextConfig} */
const nextConfig = {
    // assetPrefix: "/Avana/",
    // output: "export"
    images: {
        domains: ['35.232.216.253', '127.0.0.1'],
    },
    
}

module.exports = nextConfig
